def two_sum(nums, target):
    seen = {}  # value -> index
    for i, v in enumerate(nums):
        need = target - v
        if need in seen:
            return [seen[need], i]
        seen[v] = i
    return None

# Example
print(two_sum([2,7,11,15], 9))  # [0,1]
